# ─── Supabase ───────────────────────────────────────────────
# URL + publishable van al browser (NEXT_PUBLIC_). El secret nunca.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
SUPABASE_SECRET_KEY=sb_secret_xxx
SUPABASE_JWKS_URL=https://your-project.supabase.co/auth/v1/.well-known/jwks.json

# ─── Telegram Bot ───────────────────────────────────────────
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=your-bot-username

# ─── MGP Proxy ──────────────────────────────────────────────
MGP_PROXY_URL=https://proxy.example.com
MGP_PROXY_TOKEN=your-proxy-token

# ─── MGP Oracle ─────────────────────────────────────────────
MGP_ORACLE_URL=https://oracle.example.com
MGP_ORACLE_TOKEN=your-oracle-token

# ─── Microsoft Clarity — project ID from https://clarity.microsoft.com/
NEXT_PUBLIC_CLARITY_PROJECT_ID=your-clarity-project-id

# ─── Google Analytics 4 — measurement ID from GA admin ──────
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ─── Google AdSense — ID de bloque (panel AdSense → Anuncios → Por bloque de anuncios)
# Vacío = sin anuncio de Google en ese lugar, salvo arrivals/favoritos/recorrido
# que caen al de CONSULTAR si está definido.
# En Docker estas NEXT_PUBLIC_* tienen que ir como build args (se inlinean al build).
NEXT_PUBLIC_ADSENSE_SLOT_CONSULTAR=
# Sheet de arribos (después del primer horario). Vacío = usa CONSULTAR.
NEXT_PUBLIC_ADSENSE_SLOT_ARRIVALS=
# Debajo del header en /favoritos. Vacío = usa CONSULTAR.
NEXT_PUBLIC_ADSENSE_SLOT_FAVORITOS=
# Después del resumen en /recorrido/[linea]. Vacío = usa CONSULTAR.
NEXT_PUBLIC_ADSENSE_SLOT_RECORRIDO=

# ─── Feature Flags ──────────────────────────────────────────
NEXT_PUBLIC_USE_STATIC_REFERENCE=true

# Reseñas fake en memoria (sin Supabase, sin login real) para probar la UI
# de /recorrido en dev. Nunca se activa en producción aunque quede en true.
NEXT_PUBLIC_MOCK_REVIEWS=false

# ─── API URLs ───────────────────────────────────────────────
NEXT_PUBLIC_CUANDO_API_URL=https://bondi.example.com
NEXT_PUBLIC_PROXY_API_URL=https://proxy.example.com
NEXT_PUBLIC_BONDI_API_URL=https://bondi.example.com

# ─── Push Notifications (VAPID) ─────────────────────────────
NEXT_PUBLIC_VAPID_PUBLIC=your-vapid-public-key

# ─── MGP Crypto ─────────────────────────────────────────────
MGP_RSA_PUBKEY=your-rsa-public-key
MGP_SHARED_KEY=your-shared-key

# ─── Ads slot (MercadoPago) ─────────────────────────────────
# Public site URL. HTTPS in production; http://localhost:3000 locally.
NEXT_PUBLIC_APP_URL=http://localhost:3000
MERCADOPAGO_ACCESS_TOKEN=TEST-xxxx
MERCADOPAGO_WEBHOOK_SECRET=your-webhook-secret
# Optional. Defaults: floor 1000, step 1000 (ARS).
# AD_SLOT_FLOOR_ARS=1000
# AD_SLOT_STEP_ARS=1000

# ─── OSRM Routing ───────────────────────────────────────────
OSRM_URL=https://osrm.example.com